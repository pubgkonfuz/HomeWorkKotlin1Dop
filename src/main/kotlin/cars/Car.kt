package cars

class Car {
    fun cars(classCars: Cars) {
        println("cars carMake: ${classCars.carMake} \n cars model: ${classCars.model} \n cars engine:${classCars.engine} \n cars color: ${classCars.color}")
    }
}